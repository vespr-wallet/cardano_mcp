{
  "name": "@vespr/cardano-mcp",
  "version": "0.1.1",
  "description": "MCP server for Cardano wallet operations via VESPR",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "cardano-mcp": "./dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "tsc",
    "build": "npm run format:check && tsc",
    "start": "npm run build && node dist/index.js",
    "dev": "tsc --watch",
    "watch": "tsc --watch",
    "test": "NODE_OPTIONS='--experimental-vm-modules' jest",
    "test:coverage": "NODE_OPTIONS='--experimental-vm-modules' jest --coverage",
    "stress-test": "CARDANO_MCP_LOG_LEVEL=silent npx tsx scripts/stress-test.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/sensible": "^6.0.4",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "fastify": "^5.7.2",
    "lru-cache": "^11.2.4",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^22.19.5",
    "jest": "^30.2.0",
    "prettier": "^3.7.4",
    "ts-jest": "^29.4.6",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "license": "MIT"
}
